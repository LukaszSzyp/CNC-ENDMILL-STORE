import { LoginForm } from "./loginForm";
import * as loginStyles from "./styledLogin";

export const Login = () => {
  return (
    <loginStyles.Login>
      <LoginForm />
    </loginStyles.Login>
  );
};
